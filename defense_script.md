# 本科毕业答辩讲稿

## 第 1 页：标题页

各位老师好，我是简一畅。我的本科毕业论文题目是《基于符号化想象空间的大模型“以图思考”推理方法研究》，指导老师是丁尧相教授。接下来我将从研究背景、相关工作、方法、实验结果，以及结论与展望五个部分进行汇报。

## 第 2 页：大纲

这是本次汇报的大纲。我将先介绍问题和目标，再说明相关工作，接着介绍 PI-TWI 方法，之后汇报实验结果，最后给出总结与展望。

## 第 3 页：绪论

首先进入绪论部分，说明视觉规划中的感知瓶颈。

## 第 4 页：研究背景

VLM 在视觉规划任务中已经展示出很大潜力，因为它们能够同时处理图像观测和语言目标。但是在复杂场景里，模型往往不能一次性准确理解完整世界状态。如果视觉接地错误，后续即使使用正确规划器，也会得到错误计划。

“以图思考”通过局部裁剪、视觉草稿纸等方式，能够缓解这一问题。但从表中可以看到，直接输出和原生“以图思考”在 Crafter、CubeBench 等任务上仍然不稳定，说明规划中的视觉感知瓶颈依然存在。

## 第 5 页：研究目标

本文的核心目标，是把面向规划的“以图思考”形式化为逐步构建符号化想象空间的过程。这里的符号化想象空间，指的是从图像中提取出的、足以让真实规划器使用的一组符号事实。

围绕这个目标，本文验证三个观点：第一，这种形式化可以缓解 VLM 的感知瓶颈；第二，可复用的视觉模式可以降低构建成本；第三，这些模式可以由 VLM 基于在线归纳学习自主构建。本文将方法称为 PI-TWI。

## 第 6 页：相关工作

接下来介绍三类相关工作。

## 第 7 页：基于 VLM 的规划

已有工作中，PaLM-E 将视觉观测和语言指令结合，用于机器人高层规划；ReplanVLM 引入闭环视觉反馈；Reflective Planning 通过想象未来状态改进长程规划。

这些工作通常同时研究感知和规划。本文定位更集中：在真实规划器已知的前提下，重点研究视觉感知和符号接地。

## 第 8 页：“以图思考”

“以图思考”相关工作包括动态裁剪图像、使用视觉草稿纸，以及在视觉模态中模拟未来状态。本文关注的是面向规划的版本。

也就是说，方法不仅要看图，还要判断哪些视觉变量与规划有关，以及当前符号化想象空间是否足够支持规划。

## 第 9 页：面向知识获取的归纳学习

归纳学习方面，传统方法包括归纳逻辑编程，近期也有利用 VLM 生成符号提议或组合式专家的工作。本文让 VLM 同时进行感知和模式提议。

为了抑制错误模式，本文借鉴混合专家和掩码自编码器，用随机遮蔽的历史轨迹构造训练数据，并优化不同模式专家的权重。

## 第 10 页：方法

下面进入方法部分。

## 第 11 页：PI-TWI 方法概览

PI-TWI 有两条主线。第一条是推断主线：系统从当前图像出发，逐步揭示局部视觉变量，并构建内部符号化想象空间。每一步之后，规划器和充分性检查器判断当前信息是否足够；如果不够，系统继续揭示新变量，或者利用模式补全。

第二条是学习主线：历史轨迹进入回放缓冲区，VLM 从中提出候选模式。系统通过随机遮蔽已知变量，测试模式能否恢复被遮蔽事实，并据此更新权重。

## 第 12 页：符号化想象空间构建

视觉变量是可以单独检查的最小视觉单元，例如 Crafter 中的一个网格单元，或者 CubeBench 中魔方的一个小面。

一次感知操作中，揭示算子会裁剪对应区域，并调用 VLM 将其接地为符号事实。符号化想象空间包括直接揭示事实和模式补全事实。前者需要 VLM 调用，后者通过模式推断得到。充分性检查器判断这些事实是否足以支持真实规划器规划。

## 第 13 页：模式推断

本文把每个符号模式看作一个门控专家。只有当上下文满足条件时，模式才会激活，并对目标视觉变量的取值给出概率分布。

多个活跃模式会加权投票，得到未知变量的概率分布。这个分布有两个作用：如果最高概率足够高，就直接补全变量，减少 VLM 调用；如果仍需揭示，则重排序候选变量，把感知成本投入到更关键的位置。

## 第 14 页：在线归纳学习

模式库通过在线归纳学习得到。每次任务结束后，最终符号化想象空间进入回放缓冲区，VLM 基于历史样例提出候选模式。

为了训练权重，系统随机遮蔽部分已知变量，让模式专家预测被遮蔽的值，并最大化预测似然。这样，可靠模式权重升高，错误或无关模式权重降低。

## 第 15 页：实验

接下来汇报实验环境以及结果。

## 第 16 页：FrozenLake

第一个环境是 FrozenLake。智能体观察渲染网格世界，需要从起点到终点找到最短安全路径，并避免掉入坑洞。每个视觉变量是一个网格单元。

## 第 17 页：Crafter

第二个环境是 Crafter。本文将原始随机生存环境改造成确定性的资源获取和合成任务。智能体需要接地地图信息，并规划导航、收集和合成动作。

## 第 18 页：CubeBench

第三个环境是 CubeBench。任务目标是从渲染图像中重建魔方状态，供下游求解器使用。每个视觉变量是魔方的一个小面。

## 第 19 页：基线、消融实验和指标

实验比较 VLM 直接输出、原生“以图思考”和 PI-TWI。消融实验包括“无推断”和“无重加权”：前者禁用补全和重排序，后者不训练模式权重。

评价指标包括规划准确率、接地准确率和 token 成本。

## 第 20 页：准确率比较

准确率结果显示，直接使用 VLM 输出在复杂环境中表现很差，在 Crafter 和 CubeBench 中规划准确率经常为零。原生“以图思考”能改善部分任务，但主要集中在 CubeBench 这类小规模环境上，并且不同模型之间不稳定。

相比之下，PI-TWI 在三个任务和三个基础模型上都稳定提升了接地准确率和规划准确率。

## 第 21 页：效率比较

效率结果也显示 PI-TWI 能显著降低成本。在 FrozenLake 中，总 token 消耗降低 40.77%。在 Crafter 中，总 token 消耗下降到无推断消融的 63.44%。在 CubeBench 中，PI-TWI 达到了理论可降低幅度的约 42.86%。

同时，无重加权消融的 token 消耗更高，说明在线权重学习对效率提升是必要的。

## 第 22 页：分布外泛化

这页测试分布外泛化。本文先在 64 乘 64 的 Crafter 地图上学习模式和权重，然后零样本迁移到 128 乘 128 的更大地图上。

结果显示，主动揭示次数从 2349.63 下降到 1570.45。这说明学到的模式具有一定分布外泛化能力。

## 第 23 页：结论与展望

最后进入结论与展望。

## 第 24 页：总结

本文得到三点结论。第一，将面向规划的“以图思考”形式化为符号化想象空间构建，可以缓解 VLM 的感知瓶颈。第二，视觉模式可以作为可复用、可组合的工具，降低构建成本。第三，关键视觉模式可以由 VLM 基于在线归纳学习自主构建。

## 第 25 页：局限性和未来工作

本文仍然是一项概念验证研究，主要有两点局限。第一，实验聚焦于确定性视觉规划任务，还没有覆盖真实世界或随机环境。第二，本文假设视觉变量类型集合和每个变量的边界框已经提前给定。

未来工作可以将 PI-TWI 扩展到更真实、更随机的环境，并协同使用补全和重排序。对于高确定性的结构规律，用补全降低成本；对于噪声更大的规律，用重排序辅助探索，而不直接写入不确定事实。

## 第 26 页：致谢

以上就是我的本科毕业论文答辩汇报。感谢丁尧相老师的指导，感谢各位评委老师的聆听，也感谢课题组同学们的帮助。请各位老师批评指正。
